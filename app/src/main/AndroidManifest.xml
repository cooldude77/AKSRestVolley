<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.instanect.sample">

    <application
        android:name="com.instanect.sample.application.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Login Activity -->
        <activity
            android:name="com.instanect.sample.AKSRestVolleyActivity"
            android:label="TestApp"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <intent-filter>
                <!-- This Activity's name will be used for shortcut on desktop
                http://stackoverflow.com/questions/2444040/naming-my-application-in-android -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>
</manifest>
